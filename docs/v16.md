# V16 — Changelog

**Data:** Maio 2026
**Sessão:** Dashboard Velox PMO — sessão de correções e deploy

---

## 🔴 Correções Críticas

### gerencial.html — SyntaxError (tela branca)
- **Problema:** `.replace(/'/g,'\\'')`  gerava SyntaxError que impedia o script inteiro de carregar
- **Fix:** substituído por `esc(f.proj)` — função segura definida em utils.js
- **Impacto:** página voltou a carregar normalmente

### gerencial.html — openFeedPopup removido
- Função referenciava popup `#feed-popup` inexistente
- Feed items agora abrem `openProjPopup()` diretamente

### admin.html — resetData() com chaves erradas
- Estava apagando `v15_*` em vez de `v16_*`
- Corrigido para todas as 8 chaves corretas

### utils.js — XSS em renderSidebarUser()
- Nome, papel e iniciais do usuário inseridos sem escape
- Corrigido com `escHtml()` em todos os campos

---

## 🟡 Melhorias

### utils.js
- `esc()` atualizada para versão JS+HTML completa
- `_calcSimilarity()` e `_diasEntre()` centralizadas (removidas de projetos.html)
- Constantes de threshold centralizadas
- `calcRisco()` e `calcAlertaBadge()` usam as constantes
- `DEPARTAMENTOS` constante criada com 8 departamentos

### squads.html
- Estado de aba, filtro, gantt e área persistidos no localStorage
- `isOpen` por squad persistido individualmente
- `escHtml()` aplicado em campos de scope e gantt

### alertas.html
- Filtro e view persistidos no localStorage
- Botão ⌘ adicionado ao topbar
- `esc()` em todos os onclick dos cards

### index.html / projetos.html / gerencial.html
- `new Date()` substituído por `HOJE` / `HOJE_ISO` onde aplicável
- Datas de impressão (`print-*-date`) populadas corretamente

---

## 👤 Sistema de Usuários — novo formato login

### Login `usuario@departamento`
- Campo E-mail substituído por **Usuário** (`type="text"`)
- Placeholder: `nome.sobrenome@dept`
- Formulário admin dividido: campo usuário + dropdown departamento
- Detecção automática de colisão com sugestão (`joao.silva2@pmo`)

### Departamentos cadastrados
`infra` · `eletrica` · `mecanica` · `dev` · `pmo` · `doc` · `posvendas` · `implantacao`

### Usuários padrão atualizados
| Usuário | Senha inicial | Papel |
|---|---|---|
| `admin@pmo` | `admin123` | Admin |
| `editor@pmo` | `editor123` | Editor |
| `viewer@pmo` | `viewer123` | Viewer |
| `ivan.duarte@infra` | `ivan123` | Viewer |

> **Decisão:** Senhas simples são intencionais para o primeiro acesso.
> O V17 vai forçar troca de senha obrigatória no primeiro login.

---

## 🏗️ Infraestrutura

### Python instalado
- Python 3.12.10 via winget
- Aliases da Microsoft Store desativados
- Servidor de prévia: `python -m http.server 3333`

### Git + GitHub
- Repo local inicializado em `V16/`
- Remote: `github.com/daia85-prog/dashboard-velox-pmo`
- Branch principal: `main`

### Netlify conectado ao GitHub
- Site: `invent-dashboard.netlify.app`
- Deploy automático a cada push no branch `main`
- Zero drag & drop manual

### CLAUDE.md criado
- Regras do projeto documentadas para novas sessões
- Fundo aurora, padrões de código, constantes, fluxo de deploy

---

## 📋 Pendente — V17

| Feature | Descrição |
|---|---|
| 🔐 Hash de senha | SHA-256 no localStorage |
| 🔑 Troca no 1º acesso | Força nova senha com critérios visíveis |
| 💪 Indicador de força | Critérios em tempo real (maiúscula, número, especial, mínimo 8) |
| 👤 Papéis expandidos | Super Admin, Dept Admin, Editor, Viewer |
| 📋 Log de auditoria | Quem criou/editou/deletou e quando |
| 🔔 Alerta de deleção | P0 automático na Central de Alertas |
